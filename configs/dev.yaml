# Development environment defaults.
# Only values that differ from the Python defaults in src/mrta/core/config.py.
# All other settings fall through to config.py defaults.
# Override anything here with .env or shell env vars.
# Switch environment: export MRTA_ENV=dev (default)

embedding_model: sentence-transformers/all-MiniLM-L6-v2
clip_model: ViT-B-32

chunk_size: 1000
chunk_overlap: 200

log_level: DEBUG
